# US-001：金流環境變數與機密守門

**作為** 開發者  
**我想要** ECPay URL／Hash 槽位與機密守門齊全  
**以便** 建單與 Webhook 能讀 server env，且 Hash 不進 client

**輸入格式**：
- spec §4 變數：`ECPAY_MERCHANT_ID`、`ECPAY_HASH_KEY`、`ECPAY_HASH_IV`、`ECPAY_CHECKOUT_URL`、`ECPAY_QUERY_URL`、`ECPAY_RETURN_URL`、`ECPAY_CLIENT_BACK_URL`、`ECPAY_ENV`、`APP_BASE_URL`
- Stage checkout 預設值可寫在 `.env.example` 註解或預設 URL；Hash 槽位必須空字串
- 既有 `lib/security/secrets-not-leaked.test.ts` 的 `SECRET_ENV_KEYS`

**輸出格式**：
- `.env.example` 補齊上述鍵（無真實 Hash）
- 守門測試持續禁止 `NEXT_PUBLIC_ECPAY_HASH_*`／Hash 字串進 client

**驗收條件**：
- [x] `.env.example` 有 spec 列的 ECPay URL 與 Hash 空槽
- [x] 無任何 `NEXT_PUBLIC_*` 帶 HashKey／HashIV／service role
- [x] 不把公開測試特店帳密寫成程式常數
- [x] `ECPAY_QUERY_URL` 只留槽位；本任務不呼叫 QueryTradeInfo
- [x] `.env.example` 綠界註解不再寫「本版不使用」

#### 驗收說明

**整體結論**：PASS ✅

> `npx vitest run lib/security/secrets-not-leaked.test.ts` 4 passed。本任務只補槽位與守門，未建 checkout／Webhook。

---

**AC-1：.env.example 有 spec 列的 ECPay URL 與 Hash 空槽**

狀態：✅ 通過

- `.env.example` 含 `ECPAY_MERCHANT_ID`、Hash 空槽、`ECPAY_CHECKOUT_URL` Stage 預設、`ECPAY_RETURN_URL`／`ECPAY_CLIENT_BACK_URL`／`ECPAY_ENV`／`APP_BASE_URL`
- `lib/security/secrets-not-leaked.test.ts` 的 `ECPAY_EXAMPLE_KEYS` 逐鍵斷言存在且無 `NEXT_PUBLIC_` 變體

---

**AC-2：無 NEXT_PUBLIC Hash／service role**

狀態：✅ 通過

- `SECRET_ENV_KEYS` 含 `ECPAY_HASH_KEY`／`ECPAY_HASH_IV`；空槽且禁止 `NEXT_PUBLIC_ECPAY_HASH_*`
- source 掃描禁止 `process.env.NEXT_PUBLIC_…ECPAY…`；bundle 掃描禁止 Hash 識別字

---

**AC-3：不把公開測試特店帳密寫成程式常數**

狀態：✅ 通過

- `app`／`lib`／`components` 無 `3002607`／`QueryTradeInfo` 常數

---

**AC-4：ECPAY_QUERY_URL 只留槽位且不呼叫 QueryTradeInfo**

狀態：✅ 通過

- `.env.example` 的 `ECPAY_QUERY_URL=` 為空；測試斷言空字串
- 程式碼無 QueryTradeInfo 呼叫

---

**AC-5：綠界註解不再寫「本版不使用」**

狀態：✅ 通過

- `.env.example` 改為單元 4 槽位說明；測試斷言檔案不含「本版不使用」

**測試策略**：Test-After

**測試策略**：Test-After  
> 理由：改 env 範例與既有守門名單，補跑現有測試即可。

**優先級**：P0  
**相關功能**：Story 3；spec §4  
**依賴關係**：無
