# US-019：正式環境預覽警告 實作

**作為** 正式站訪客或會員  
**我想要** 開發預覽不得被當成已付款開通  
**以便** live 只跟 `access_status` 與真實報告走

**輸入格式**：
- US-018 紅燈測試
- `.env.production` 現為 `NEXT_PUBLIC_COMMERCIAL_PREVIEW=1`，本單改 `0`
- 不新增 env 名稱

**輸出格式**：
- `lib/commercial/preview.ts`／預覽條／報告頁警告
- `.env.production` 預設關閉預覽

**驗收條件**：
- [x] US-018 測試轉綠
- [x] `.env.production` 為 `0`
- [x] production＋`1` 有 alert 且無 overlay
- [x] 本機 `next dev`＋預覽 `1` 仍可 overlay
- [x] locked 的解鎖 CTA 仍走 US-015 文案（與警告並存）

#### 驗收說明

**整體結論**：PASS ✅

> `npx vitest run lib/commercial/preview-production.test.ts lib/commercial/preview.test.ts components/report/ReportCard.test.tsx components/home/HomeClient.test.tsx` 42 passed。Mutation：誤設分支 `showAlert` 改 `false` 後 US-018 警告斷言變紅，已還原。

---

**AC-1：US-018 測試轉綠**

狀態：✅ 通過

- `lib/commercial/preview.ts` 的 `resolveCommercialPreviewPolicy()`／`PRODUCTION_PREVIEW_ALERT` 已匯出

---

**AC-2：.env.production 為 0**

狀態：✅ 通過

- `.env.production` 為 `NEXT_PUBLIC_COMMERCIAL_PREVIEW=0`；`preview-production.test.ts` 斷言該行

---

**AC-3：production＋1 有 alert 且無 overlay**

狀態：✅ 通過

- 政策 `showAlert`＋`overlayEnabled=false`；`ReportCard.tsx` 渲染 `role="alert"`、不掛 `CommercialPreviewBar`

---

**AC-4：本機 next dev＋預覽 1 仍可 overlay**

狀態：✅ 通過

- 非 production 且 previewRaw `1` 時 `overlayEnabled` 為 true、無 alert；既有 ReportCard 預覽 B 測試仍綠

---

**AC-5：locked 解鎖 CTA 仍走 US-015 文案**

狀態：✅ 通過

- `ReportCard.test.tsx` 在 production＋preview 仍斷言「解鎖完整報告」與基本分析標題

**測試策略**：Test-First  
> 理由：對 US-018 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 13  
**依賴關係**：US-015、US-018
