# US-017：付款處理中頁

**作為** 從綠界按返回商店的會員  
**我想要** 只看到處理中  
**以便** 瀏覽器回跳不能當成付款成功

**輸入格式**：
- 路徑 `/orders/processing`（對齊 `ECPAY_CLIENT_BACK_URL`）
- 不讀綠界 query 當成功；不改 `access_status`
- Webhook 未到：維持處理中；**不**做 Story 11 輪詢／QueryTradeInfo
- 「返回解讀」連到 `/`

**輸出格式**：
- `app/orders/processing/page.tsx`（或同等 App Router）

**驗收條件**：
- [x] 畫面為處理中；帶綠界 query 也不更新 orders／權益
- [x] 不呼叫 QueryTradeInfo
- [x] 有返回 `/` 的連結
- [x] 本頁不解鎖，也不因綠界 query 把 `access_status` 改回 `locked`
- [x] 不新增 `reports.user_id`；文件或頁面註明頂層導轉會丟掉記憶體 `persist_id`，解鎖後若畫面清空需再 POST 同一生辰才能 GET 三欄

#### 驗收說明

**整體結論**：PASS ✅

> `npx vitest run app/orders/processing/page.test.tsx` 2 passed。頁面為 Server Component，await `searchParams` 後丟棄，無 fetch／orders／access_status 寫入。

---

**AC-1：處理中畫面；query 不更新訂單／權益**

狀態：✅ 通過

- `app/orders/processing/page.tsx` 標題「付款處理中」；測試帶 `RtnCode=1` 仍不呼叫 `fetch`

---

**AC-2：不呼叫 QueryTradeInfo**

狀態：✅ 通過

- 頁面原始碼不含 `QueryTradeInfo`

---

**AC-3：返回 `/` 連結**

狀態：✅ 通過

- `Link href="/"` 文案「返回解讀」

---

**AC-4：本頁不解鎖、不因 query 改回 locked**

狀態：✅ 通過

- 原始碼不含 `access_status`、`from("orders")`

---

**AC-5：不新增 reports.user_id；註明 persist_id 會丟**

狀態：✅ 通過

- 頁面無 `user_id`；內文說明頂層導轉會丟掉記憶體 persist_id

**測試策略**：Test-After  
> 理由：靜態處理中頁以畫面為主；不解鎖可用手動或輕量測試確認無寫入。

**優先級**：P0  
**相關功能**：Story 4  
**依賴關係**：無
