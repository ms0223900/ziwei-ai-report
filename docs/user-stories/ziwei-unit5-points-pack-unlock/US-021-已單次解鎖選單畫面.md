# US-021：已單次解鎖選單畫面

**作為** 已用點解鎖過報告的會員  
**我想要** 導覽或報告區的簡單選單  
**以便** 重整或重登後仍能開啟那些報告的進階

**輸入格式**：
- US-020 選單 API；US-015 進階 GET；US-018 已改的 `HomeClient.loadAdvanced`（選單點選不得另寫一套終身-only 閘門）
- 位置：簡單選單，不是獨立產品頁、不是篩選器
- 項目至少能打開對應 `persist_id` 的進階
- 建議槽位 `slot-report-unlocks`

**輸出格式**：
- 選單元件（導覽或報告區）＋接上 `HomeClient` GET

**驗收條件**：
- [x] 選單含已單點解鎖的報告；點選後可再開進階
- [x] 終身開通時選單不倒入全部 `reports`
- [x] 未綁 `user_id` 的舊列不出現
- [x] 重整／重登後仍在（讀 API，不靠記憶體）

#### 驗收說明

**整體結論**：PASS ✅

> 全套 `npx vitest run` 314 passed／1 skipped；`tsc --noEmit`、`eslint .`、`npm run build` 通過。新增 `components/home/HomeClient.test.tsx`（選單 5 則）、`components/home/ReportUnlocksMenu.test.ts`（2 則）。既有 5 則 fetch 次數斷言改為排除 `/api/report-unlocks` 後計數（選單會自行載入）。未在真實瀏覽器＋Supabase 走過排版。

---

**AC-1：選單含已單點解鎖的報告；點選後可再開進階**

狀態：✅ 通過

- `components/home/ReportUnlocksMenu.tsx`（`slot-report-unlocks`）列出「暱稱・日期」按鈕；`HomeClient.openUnlockedReport()` 走與單點解鎖相同的擁有者 GET `/api/reports/[persistId]`，成功後以該報告的基本＋進階內容切到報告畫面，沒有另寫終身閘門

---

**AC-2：終身開通時選單不倒入全部 reports**

狀態：✅ 通過

- 選單只渲染 `/api/report-unlocks` 回傳的項目；API 端只讀 `report_unlocks`（US-020）

---

**AC-3：未綁 user_id 的舊列不出現**

狀態：✅ 通過

- 由 US-020 API 過濾（`reports.user_id` 必須等於 session）；畫面不自行組清單

---

**AC-4：重整／重登後仍在（讀 API，不靠記憶體）**

狀態：✅ 通過

- 已登入時 `HomeClient` 掛載即 GET `/api/report-unlocks`；單點解鎖成功後重新讀一次，新報告立刻出現在選單；訪客不打 API、不顯示選單

**測試策略**：Test-After  
> 理由：選單排版以畫面為主；資料契約已由 US-019／US-020 鎖住。

**優先級**：P0  
**相關功能**：Story 10  
**依賴關係**：US-015、US-018、US-020
