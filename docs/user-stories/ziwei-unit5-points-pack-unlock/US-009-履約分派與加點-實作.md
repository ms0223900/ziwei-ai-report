# US-009：履約分派與加點 實作

**作為** 綠界 ReturnURL  
**我想要** 已履約才跳過，並依 `orders.plan_id` 分派  
**以便** 點數包只加點、終身方案只開通

**輸入格式**：
- US-008 紅燈測試
- US-004 `fulfill_points_pack_order`
- 現況：`status==="paid"` 只跑 `unlockIfLocked` 後整段 return — **必須刪除**此終身-only 早退
- unique 衝突視為已履約並回 `1|OK`，不得回 `0|Error`

**輸出格式**：
- `app/api/payments/ecpay/webhook/route.ts`（`OrderRow` 含 `plan_id`）
- 呼叫加點 RPC；終身仍只 `unlockIfLocked`

**驗收條件**：
- [x] US-008 測試轉綠
- [x] 點數包成功不寫 `access_status`／`subscription_status`／`reports.status`
- [x] 終身成功不寫 `points_balance`／`point_transactions`
- [x] 寫入成功後才回 `1|OK`；不重做驗簽
- [x] 未套用 US-003／US-004 遷移前，不得勾 Story 4／5 金流加點 AC；fake `.rpc()` 轉綠不算加點履約

#### 驗收說明

**整體結論**：PASS ✅

> 已付或本筆標 paid 之後才依 `orders.plan_id` 分派。點數包只呼叫 `fulfill_points_pack_order`；終身仍只 `unlockIfLocked`。未知方案回 `1|OK` 且不改權益。

---

**AC-1：US-008 測試轉綠**

狀態：✅ 通過

- `npx vitest run app/api/payments/ecpay/webhook/route.test.ts`：21 passed

---

**AC-2：點數包成功不寫 access_status／subscription_status／reports.status**

狀態：✅ 通過

- `app/api/payments/ecpay/webhook/route.ts` 的 `POST()` 在 `plan_id === points_pack_5` 時只呼叫 `fulfillPointsPack()`，不進 `unlockIfLocked()`
- 測試斷言 `access_status` 維持 locked、`subscription_status` 不變；本路徑沒有寫 `reports`

---

**AC-3：終身成功不寫 points_balance／point_transactions**

狀態：✅ 通過

- 終身分支只 `unlockIfLocked()`；測試斷言餘額 0、帳本與 `report_unlocks` 為空

---

**AC-4：寫入成功後才回 1|OK；不重做驗簽**

狀態：✅ 通過

- 加點 RPC 或終身開通失敗回 `0|Error`；成功才 `1|OK`
- 驗簽仍是既有 `verifyCheckMacValue()`，本任務沒有改演算法

---

**AC-5：fake rpc 轉綠不算 Story 4／5 金流加點**

狀態：✅ 通過

- 測試注入的 `.rpc()` 只證明分派呼叫；未把 Story 4／5 的真實金流加點勾成完成

**測試策略**：Test-First  
> 理由：對 US-008 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 4／5／6  
**依賴關係**：US-004、US-008
