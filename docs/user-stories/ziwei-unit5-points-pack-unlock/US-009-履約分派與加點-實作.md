# US-009：履約分派與加點 實作

**作為** 綠界 ReturnURL  
**我想要** 已履約才跳過，並依 `orders.plan_id` 分派  
**以便** 點數包只加點、終身方案只開通

**輸入格式**：
- US-008 紅燈測試
- US-004 `fulfill_points_pack_order`
- 現況：`status==="paid"` 只跑 `unlockIfLocked` 後整段 return — **必須刪除**此終身-only 早退
- unique 衝突視為已履約並回 `1|OK`，不得回 `0|Error`

**輸出格式**：
- `app/api/payments/ecpay/webhook/route.ts`（`OrderRow` 含 `plan_id`）
- 呼叫加點 RPC；終身仍只 `unlockIfLocked`

**驗收條件**：
- [ ] US-008 測試轉綠
- [ ] 點數包成功不寫 `access_status`／`subscription_status`／`reports.status`
- [ ] 終身成功不寫 `points_balance`／`point_transactions`
- [ ] 寫入成功後才回 `1|OK`；不重做驗簽
- [ ] 未套用 US-003／US-004 遷移前，不得勾 Story 4／5 金流加點 AC；fake `.rpc()` 轉綠不算加點履約

**測試策略**：Test-First  
> 理由：對 US-008 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 4／5／6  
**依賴關係**：US-004、US-008
