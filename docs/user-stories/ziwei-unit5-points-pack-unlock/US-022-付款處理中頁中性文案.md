# US-022：付款處理中頁中性文案

**作為** 從綠界返回商店的會員  
**我想要** 處理中頁只說等待伺服器通知  
**以便** 不會以為「再送同一生辰就能看進階」

**輸入格式**：
- spec 第 7 節問題 5：現況 `app/orders/processing/page.tsx` 教導回跳後再 POST 同一生辰
- 本單終身／單點 GET 都要 `reports.user_id`；訪客列再生成是**新** `reports.id`
- 改為中性「處理中，請返回後查看餘額／解鎖」；**不**依網址參數更新權益；不呼叫 QueryTradeInfo

**輸出格式**：
- `app/orders/processing/page.tsx` 與既有 `page.test.tsx`

**驗收條件**：
- [x] 畫面仍為處理中；不讀綠界 query 當成功
- [x] 文案不再出現「再送同一生辰」／用 persist_id 記憶體當進階捷徑
- [x] 引導返回後查看餘額或解鎖狀態
- [x] 不實作輪詢或 QueryTradeInfo

#### 驗收說明

**整體結論**：PASS ✅

> `npx vitest run app/orders`：2 passed；`eslint` 無錯誤。產品碼（`app`／`components`／`lib`）已無「同一生辰」字樣。

---

**AC-1：畫面仍為處理中；不讀綠界 query 當成功**

狀態：✅ 通過

- `app/orders/processing/page.tsx` 標題仍「付款處理中」，`searchParams` 只 await 不使用；測試帶 `RtnCode=1`／`SimulatePaid=1` 仍是處理中、無 fetch

---

**AC-2：文案不再出現「再送同一生辰」／用 persist_id 記憶體當進階捷徑**

狀態：✅ 通過

- 移除 persist_id 與重送生辰段落；測試斷言頁面文字不含「同一生辰」「persist_id」

---

**AC-3：引導返回後查看餘額或解鎖狀態**

狀態：✅ 通過

- 新文案：伺服器收到通知後才加點或開通，請返回報告頁查看點數餘額與解鎖狀態，並指向「已用點數解鎖的報告」選單

---

**AC-4：不實作輪詢或 QueryTradeInfo**

狀態：✅ 通過

- 頁面是無狀態 server component；原始碼斷言不含 `QueryTradeInfo`、`setInterval`／`setTimeout`／`useEffect`

**測試策略**：Test-After  
> 理由：文案調整；更新既有處理中頁測試即可。

**優先級**：P0  
**相關功能**：Story 9／13；spec 第 7 節問題 5  
**依賴關係**：無
