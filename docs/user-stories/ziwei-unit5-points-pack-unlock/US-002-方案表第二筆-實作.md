# US-002：方案表第二筆 實作

**作為** 系統  
**我想要** 伺服器端方案表同時有終身與點數包  
**以便** checkout 只認後端常數、前端無法改價

**輸入格式**：
- US-001 紅燈測試
- spec 共用方案表兩列；加點數 5 為後端常數
- `lib/payments/plans.ts` 現為 `server-only`；client CTA 不可 import 此檔

**輸出格式**：
- `lib/payments/plans.ts`（`CheckoutPlan` 涵蓋兩方案）
- 可另抽無 secret 的 plan id 常數給 client（例如 `lib/payments/plan-ids.ts`）

**驗收條件**：
- [ ] US-001 測試轉綠
- [ ] 未知 plan 不回金額
- [ ] 不實作第二種點數包或訂閱方案列
- [ ] 可不抽 client `plan-ids.ts`；US-018 允許寫死 `points_pack_5` 同一字串

**測試策略**：Test-First  
> 理由：對 US-001 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 1  
**依賴關係**：US-001
