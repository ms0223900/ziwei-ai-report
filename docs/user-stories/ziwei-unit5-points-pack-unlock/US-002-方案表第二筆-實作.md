# US-002：方案表第二筆 實作

**作為** 系統  
**我想要** 伺服器端方案表同時有終身與點數包  
**以便** checkout 只認後端常數、前端無法改價

**輸入格式**：
- US-001 紅燈測試
- spec 共用方案表兩列；加點數 5 為後端常數
- `lib/payments/plans.ts` 現為 `server-only`；client CTA 不可 import 此檔

**輸出格式**：
- `lib/payments/plans.ts`（`CheckoutPlan` 涵蓋兩方案）
- 可另抽無 secret 的 plan id 常數給 client（例如 `lib/payments/plan-ids.ts`）

**驗收條件**：
- [x] US-001 測試轉綠
- [x] 未知 plan 不回金額
- [x] 不實作第二種點數包或訂閱方案列
- [x] 可不抽 client `plan-ids.ts`；US-018 允許寫死 `points_pack_5` 同一字串

#### 驗收說明

**整體結論**：PASS ✅

> `CheckoutPlan` 改為終身／點數包聯集；`resolveCheckoutPlan("points_pack_5")` 回 49／TWD／紫微斗數點數包（5 點）／`creditPoints: 5`。未抽 `plan-ids.ts`。US-001 測試已轉綠。

---

**AC-1：US-001 測試轉綠**

狀態：✅ 通過

- `npx vitest run lib/payments/plans.test.ts`：3 passed
- `lib/payments/plans.ts` 的 `resolveCheckoutPlan()` 辨識 `points_pack_5`

---

**AC-2：未知 plan 不回金額**

狀態：✅ 通過

- 未知 id 仍回 `null`；`app/api/payments/checkout/route.test.ts` 的 `plan_id: "points_pack"` 仍 400、不建單

---

**AC-3：不實作第二種點數包或訂閱方案列**

狀態：✅ 通過

- 目錄僅終身與 `points_pack_5` 兩筆；無訂閱／第二 pack

---

**AC-4：可不抽 client plan-ids.ts**

狀態：✅ 通過

- 未新增 `lib/payments/plan-ids.ts`；client 仍不可 import `server-only` 的 `plans.ts`

**測試策略**：Test-First  
> 理由：對 US-001 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 1  
**依賴關係**：US-001
