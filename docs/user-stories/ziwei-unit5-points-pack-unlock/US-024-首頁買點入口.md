# US-024：首頁買點入口

**作為** 已登入會員  
**我想要** 在首頁（還沒產生報告前）就能看到點數餘額並購買點數包  
**以便** 不必先產生一份報告才找得到買點入口

**輸入格式**：
- SSR `initialPointsBalance`（US-018 已由 `app/page.tsx` 傳入 `HomeClient`）
- 買點沿用 `UnlockCheckoutCta`：只送 `{ "plan_id": "points_pack_5" }`；不另寫付款流程
- 位置：首頁生辰表單畫面上方的一列；報告畫面維持 US-018 的報告區買點，不重複顯示
- 槽位 `slot-home-points-pack`

**輸出格式**：
- `components/home/HomePointsBar.tsx`（或等價）＋接到 `HomeClient` 表單畫面
- 文案：「目前點數：N 點」＋「購買點數包」

**驗收條件**：
- [x] 已登入時，首頁表單畫面顯示點數餘額與「購買點數包」
- [x] 點「購買點數包」送 `POST /api/payments/checkout` `{ "plan_id": "points_pack_5" }`
- [x] 訪客首頁不顯示這一列
- [x] 報告畫面不重複出現這一列（報告區買點不變）
- [x] 餘額只來自 SSR `profiles.points_balance`：付款回到 `/` 重新載入後顯示新餘額；前端不自行加點

#### 驗收說明

**整體結論**：PASS ✅

> `components/home/HomeClient.test.tsx`「首頁買點入口（US-024）」4 則；全套 320 passed／1 skipped；`tsc`、`eslint .`、`npm run build` 通過。未在真實瀏覽器看過排版。

---

**AC-1：已登入時，首頁表單畫面顯示點數餘額與「購買點數包」**

狀態：✅ 通過

- `components/home/HomePointsBar.tsx`（`slot-home-points-pack`）顯示「目前點數：N 點」與買點按鈕；`HomeClient` 在已登入且為表單／生成中畫面時渲染；locked 與終身會員皆顯示

---

**AC-2：點「購買點數包」送 checkout `{ "plan_id": "points_pack_5" }`**

狀態：✅ 通過

- 沿用 `UnlockCheckoutCta`（`planId=POINTS_PACK_CLIENT_PLAN_ID`），測試斷言 body 只有 `plan_id: "points_pack_5"`

---

**AC-3：訪客首頁不顯示這一列**

狀態：✅ 通過

- 條件含 `hasSession`；訪客無此區塊、無買點按鈕

---

**AC-4：報告畫面不重複出現這一列**

狀態：✅ 通過

- 報告畫面不渲染 `HomePointsBar`；測試斷言「購買點數包」只剩報告區一顆

---

**AC-5：餘額只來自 SSR `profiles.points_balance`**

狀態：✅ 通過

- 顯示值為 `HomeClient` 的 `pointsBalance`，初值來自 `app/page.tsx` 的 `ensureProfile().points_balance`；前端只在單點解鎖成功時以 API 回傳的新餘額覆寫，不自行加點

**測試策略**：Test-After  
> 理由：顯示條件與送出內容單純，入口排版以畫面回饋為主；互動以 `HomeClient.test.tsx` 補測。

**優先級**：P1  
**相關功能**：Story 3（已開通不隱藏買點）；US-018 延伸  
**依賴關係**：US-007、US-018
