# US-007：點數包建單與閘門 實作

**作為** 已登入會員  
**我想要** 只送 `points_pack_5` 就能建 TWD 49 訂單  
**以便** 終身解鎖與點數包可並存

**輸入格式**：
- US-006 紅燈測試
- US-002 方案表
- 今日 HEAD：未知 `plan_id` 先 400，故 `points_pack_5` 在入表前是 400 不是 409
- US-002 之後、未改閘門前：已解析的 plan 對 `unlocked` **一律** 409 — 必須先 `resolveCheckoutPlan` 再依 `plan_id` 決定閘門

**輸出格式**：
- `app/api/payments/checkout/route.ts`（不另開單元 4 任務）

**驗收條件**：
- [ ] US-006 測試轉綠
- [ ] 點數包即使已永久解鎖仍建 pending 並回綠界欄位
- [ ] 終身方案已開通仍 409
- [ ] 不重做簽章／Tunnel

**測試策略**：Test-First  
> 理由：對 US-006 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 1／2  
**依賴關係**：US-006
