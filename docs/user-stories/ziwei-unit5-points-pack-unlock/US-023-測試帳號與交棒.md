# US-023：測試帳號與交棒

**作為** 講師／學員  
**我想要** 兩個固定測試帳號與欄位交棒清單  
**以便** 單元 7／8 能讀同一套資料驗重送與不足

**輸入格式**：
- spec Story 13：帳號 A `points_balance=5`、目標報告 `user_id=A` 且無該列 `report_unlocks`；帳號 B 餘額 0
- 單元 7 只讀：`orders.plan_id/status/amount/merchant_trade_no`、`points_balance`、帳本列、`report_unlocks`
- 單元 8 用本版資料驗重送、扣點成功、不足；本單不寫完整驗測劇本
- 課堂規則：訪客報告須登入後**新產生**；grant ≠ 加點 ≠ 任意 uuid 通行證；點數包驗收禁用 grant
- US-022 回跳文案已中性

**輸出格式**：
- howto（建議 `docs/user-stories/ziwei-unit5-points-pack-unlock/howto-points-pack.md`）
- 可附 seed／Checkpoint 說明；不必每次現場付款
- 本任務不改產品行為

**驗收條件**：
- [x] 文件能依帳號 A 走 Story 8 Happy（有餘額、自己的報告）
- [x] 文件能依帳號 B 走 Story 11 不足阻擋
- [x] 列出單元 7／8 可讀的訂單、餘額、帳本、選單四類欄位
- [x] 寫明 grant 不算加點、進階仍要 `reports.user_id`；權益鍵是 `persist_id`
- [x] 寫明不可再用「再送同一生辰」當終身／單點進階捷徑；單元 4 SOP 同句在本單元作廢

#### 驗收說明

**整體結論**：PASS ✅

> 文件產出：`howto-points-pack.md`。本任務不改產品行為。文件中的 RPC reason（`credited`／`already_fulfilled`／`unlocked`／`already_unlocked`／`insufficient`／`forbidden`）、守門 trigger 與欄位名，已對照 `supabase/migrations/2026092100000*.sql` 與現行 API。§3.2 路線 2 的 Checkpoint SQL 沒有在真實 Supabase 專案跑過，課前需講師實跑一次。

---

**AC-1：文件能依帳號 A 走 Story 8 Happy（有餘額、自己的報告）**

狀態：✅ 通過

- §3 準備 A（沙盒付款或 Checkpoint SQL 呼叫 `fulfill_points_pack_order`），§4 逐步操作與預期資料列（餘額 5→4、debit 列、`report_unlocks` 一列、選單出現、重送 `already_unlocked`）

---

**AC-2：文件能依帳號 B 走 Story 11 不足阻擋**

狀態：✅ 通過

- §5：點擊前即顯示不足提示、API 回 `insufficient`、無扣點無解鎖列；另附用 B 解鎖 A 報告回 `forbidden` 的反例

---

**AC-3：列出單元 7／8 可讀的訂單、餘額、帳本、選單四類欄位**

狀態：✅ 通過

- §6 四類表格，並列單元 8 重送／扣點成功／不足三件建議驗測

---

**AC-4：寫明 grant 不算加點、進階仍要 reports.user_id；權益鍵是 persist_id**

狀態：✅ 通過

- §1 課堂規則表逐條寫明，並註明點數包驗收禁用 grant

---

**AC-5：寫明不可再用「再送同一生辰」當終身／單點進階捷徑；單元 4 SOP 同句在本單元作廢**

狀態：✅ 通過

- §1 最後一列指明單元 4 `howto-ecpay-sandbox.md` §1、§6-A 第 5 步作廢；單元 4 howto §1 開頭也加了一行作廢指引連回本文件

---

**後續建議**（選填）

- 課前在目標 Supabase 專案實跑一次 §3.2 路線 2，確認 `set_config` 能讓 `auth.role()` 回 `service_role`；若專案的 `auth.role()` 實作不同，改走路線 1（沙盒付款）。

**測試策略**：Exploratory  
> 理由：交棒與課堂資料是文件產出，無產品邏輯可先紅。

**優先級**：P0  
**相關功能**：Story 13；spec 第 7 節問題 5  
**依賴關係**：US-009、US-013、US-021、US-022
