# US-017：會員視圖第三態 實作

**作為** 系統  
**我想要** `resolveMembershipView` 能表達單點解鎖第三態  
**以便** 畫面只消費旗標，不把單點畫成永久開通

**輸入格式**：
- US-016 紅燈測試
- spec 第 7 節問題 3：必須改 view，只改 GET 不夠

**輸出格式**：
- `lib/membership/view.ts`（及型別）
- 不在本任務改完整畫面排版（US-018）

**驗收條件**：
- [x] US-016 測試轉綠
- [x] 單點第三態不丟進階 payload、不設終身 CTA
- [x] 終身仍 `showCta=false`（終身主按鈕），買點旗標可獨立為 true

#### 驗收說明

**整體結論**：PASS ✅

> `npx vitest run lib/membership`：16 passed；全套 290 passed／1 skipped；`tsc --noEmit`、`eslint` 無錯誤。畫面尚未消費新旗標（US-018）。

---

**AC-1：US-016 測試轉綠**

狀態：✅ 通過

- 8 則紅燈全轉綠；US-016 測試內的區域型別擴充已移除，改用正式 `ResolveMembershipViewInput`

---

**AC-2：單點第三態不丟進階 payload、不設終身 CTA**

狀態：✅ 通過

- `lib/membership/view.ts` 的 `resolveMembershipView()`：session＋`isOwnReport`＋`unlockMode: "points"` 回進階標題、帶 `advanced`、`showCta=false`、`ctaLabel` 為 `MEMBERSHIP_CTA_POINT_UNLOCKED`（「已用 1 點解鎖此報告」），不是「已開通」

---

**AC-3：終身仍 showCta=false（終身主按鈕），買點旗標可獨立為 true**

狀態：✅ 通過

- `accessStatus === "unlocked"` 一律 `unlockMode: "lifetime"`、`showCta=false`、`showPointsPackCta=true`；所有已登入分支共用 `memberFlags`，訪客 `purchaseRequiresLogin=true`

**測試策略**：Test-First  
> 理由：對 US-016 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 3／9  
**依賴關係**：US-016
