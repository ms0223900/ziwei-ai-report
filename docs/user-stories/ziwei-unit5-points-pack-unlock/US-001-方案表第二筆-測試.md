# US-001：方案表第二筆 測試

**作為** 開發者  
**我想要** 先有會失敗的第二筆方案測試  
**以便** 金額／品名／加點數由後端決定

**輸入格式**：
- 既有 `lib/payments/plans.ts`：目前只認 `unlock_report_lifetime`
- 定稿：`points_pack_5`、`amount=49`、`currency=TWD`、`ItemName`／`TradeDesc=紫微斗數點數包（5 點）`、履約加 5 點
- 終身方案維持 99／紫微斗數完整解讀
- 未知 `plan_id` → `null`、不給價
- `CheckoutPlan` 不得再寫死只允許終身方案

**輸出格式**：
- 對應 `*.test.ts`（建議擴充 `lib/payments/plans.test.ts`）

**驗收條件**：
- [x] 聚焦測試因功能尚未實作而預期紅燈
- [x] 斷言 `points_pack_5` 為 49／TWD／紫微斗數點數包（5 點）／加 5 點
- [x] 斷言終身方案仍為 99／紫微斗數完整解讀
- [x] 斷言未知 plan 失敗
- [x] 不斷言第二種點數包或訂閱方案

#### 驗收說明

**整體結論**：PREPARED：預期紅燈測試已建立

> `npx vitest run lib/payments/plans.test.ts`：3 tests，1 failed／2 passed。待 US-002 轉綠。

路徑：`lib/payments/plans.test.ts`  
原因：`resolveCheckoutPlan("points_pack_5")` 回 `null`（方案表尚未有第二筆），非測試語法／import 錯誤。終身與未知 plan 既有斷言仍綠。

---

**AC-1：聚焦測試因功能尚未實作而預期紅燈**

狀態：✅ 通過（測試任務 AC）

- 失敗訊息：`expected null to deeply equal { planId: 'points_pack_5', … }`

---

**AC-2：斷言 points_pack_5 為 49／TWD／紫微斗數點數包（5 點）／加 5 點**

狀態：✅ 通過（測試任務 AC）

- `lib/payments/plans.test.ts` 對 `points_pack_5` 斷言 `amount: 49`、`currency: "TWD"`、品名／交易描述、`creditPoints: 5`

---

**AC-3：斷言終身方案仍為 99／紫微斗數完整解讀**

狀態：✅ 通過（測試任務 AC）

- 既有 `unlock_report_lifetime` 案例維持 99／TWD／紫微斗數完整解讀，本圈仍通過

---

**AC-4：斷言未知 plan 失敗**

狀態：✅ 通過（測試任務 AC）

- `resolveCheckoutPlan("not_a_supported_plan")` 仍斷言 `null`

---

**AC-5：不斷言第二種點數包或訂閱方案**

狀態：✅ 通過（測試任務 AC）

- 測試未把其他 pack／訂閱 id 列為成功方案

**測試策略**：Test-First（測試準備）  
> 理由：方案常數是明確 input／output，適合先紅後綠。

**優先級**：P0  
**相關功能**：Story 1  
**依賴關係**：無
