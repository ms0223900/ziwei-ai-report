# US-002：次要區點數追問與訂閱入口

**作為** 訪客  
**我想要** 在主 CTA 下方看到鎖定追問框與訂閱入口  
**以便** 知道點數只買追問、訂閱看進階＋月額度，且兩者都尚未開放

**輸入格式**：
- 槽位：`slot-followup`、`slot-subscribe`（常數來自 US-001）
- 追問：`placeholder`「追問需點數或訂閱」；說明「點數只買 1 次追問；不解鎖報告、不重算命盤」
- 訂閱：次要按鈕或文字鏈「了解訂閱權益」；說明「訂閱有效可看進階報告，本月可追問 10 次；第一版不做月報」
- 三種模式各一句：單次解鎖看進階不附贈追問；點數只買 1 次追問；訂閱看進階＋本月追問 10 次
- 即將開放文案與主 CTA 同款：「解鎖即將開放，本版不收費。」
- 鎖定框：`readOnly`＋點擊／focus 處理，禁止 `disabled`

**輸出格式**：
- 次要區元件（可放 `components/report/`）掛在主 CTA 下方
- 更新 `ReportCard.test.tsx`：畫面 A 允許 readOnly 追問框；點追問／訂閱不改標題、不呼叫 fetch

**驗收條件**：
- [x] 主 CTA 下方可見 `slot-followup` 與 `slot-subscribe`，且不是與主 CTA 同等級的三個並列主按鈕
- [x] 追問框 `readOnly`、不可輸入，點擊或 focus 顯示即將開放說明
- [x] 點「了解訂閱權益」顯示即將開放說明，不開結帳
- [x] 三種模式各一句產品狀態可見
- [x] 點擊不觸發 `fetch`／`requestReport`，標題仍為「{暱稱}的基本分析」
- [x] 本任務不實作預覽 B／C／D

#### 驗收說明

**整體結論**：PASS ✅

> 次要區掛在主 CTA 下方；追問為 `readOnly` 文字鏈訂閱。`npx vitest run components/report/ReportCard.test.tsx` 通過。

---

**AC-1：槽位可見且弱於主 CTA**

狀態：✅ 通過

- `CommercialSecondaryZone` 用 `aside` 與文字鏈，主 CTA 仍是 `bg-seal` 主按鈕
- 兩槽位 `data-report-slot` 已掛上

---

**AC-2：追問框 readOnly 可點出即將開放**

狀態：✅ 通過

- `input` 為 `readOnly`、非 `disabled`；click／focus 顯示 `UPCOMING_UNLOCK_NOTE`

---

**AC-3：訂閱入口即將開放**

狀態：✅ 通過

- 「了解訂閱權益」`type="button"`，只改本地 note

---

**AC-4：三種模式各一句**

狀態：✅ 通過

- 渲染 `MODE_UNLOCK_LINE`／`MODE_CREDIT_LINE`／`MODE_SUBSCRIBE_LINE`

---

**AC-5：不呼叫 fetch、標題不變**

狀態：✅ 通過

- `ReportCard.test.tsx` spy `fetch` 未被呼叫；標題仍「小圓的基本分析」

---

**AC-6：不實作預覽 B／C／D**

狀態：✅ 通過

- 本任務未加預覽條或四態切換

**測試策略**：Test-After
> 理由：次要區以文案層級與點擊提示為主，屬 UI 交互，補元件測試即可。

**優先級**：P0  
**相關功能**：Story 3；spec 第 7 節問題 6（畫面 A 追問框）  
**依賴關係**：US-001
