# US-004：預覽態解析實作

**作為** 系統  
**我想要** 用純函式算出預覽是否開啟與四態視圖  
**以便** UI 只消費旗標，不把 query 或 storage 當成付款結果

**輸入格式**：
- US-003 的函式契約與紅燈測試
- env 公開標記名稱：`NEXT_PUBLIC_COMMERCIAL_PREVIEW`（本任務只解析字串，不改 Route）

**輸出格式**：
- `lib/commercial/preview.ts`（或 US-003 測試所對路徑）
- 不讀 `localStorage`／`sessionStorage`
- 不 fetch、不寫 `reports`

**驗收條件**：
- [ ] US-003 測試轉綠
- [ ] `enabled=false` 時忽略 `localState` 與 query，有效態為 `A`
- [ ] query `preview`／`tier`／`status` 不改變有效態
- [ ] B／C／D 範例文案含「預覽用範例」，函式不讀 `advanced_json`
- [ ] 不改 `buildReportResponse.ts`、不新增 API

**測試策略**：Test-First
> 理由：對 US-003 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 4；spec 第 7 節問題 1／2／4／5  
**依賴關係**：US-003
