# US-016：會員視圖訂閱態 測試

**作為** 開發者  
**我想要** 先有會失敗的 `resolveMembershipView` 訂閱分支測試  
**以便** 畫面旗標在訂閱有效時正確

**輸入格式**：
- `lib/membership/view.ts`、`view.test.ts`
- spec §2 Story 7 view 規則

**輸出格式**：
- 擴充 `lib/membership/view.test.ts`

**驗收條件**：
- [ ] 聚焦測試因功能尚未實作而預期紅燈（失敗原因是功能缺失，不是語法／import 錯誤）
- [ ] `hasActiveSubscription && isOwnReport` 時：`unlockMode="subscription"`、`advancedLocked=false`、`showCta=false`、`showUnlockWithPoint=false`、`pointsInsufficient=false`，`ctaLabel` 含「訂閱有效至」
- [ ] 順序：lifetime → points → subscription；lifetime 或 points 同時成立時不走 subscription
- [ ] 訂閱失效時回到單元 5 的鎖定分支（S7-6：點數 ≥ 1 顯示用點解鎖）

**測試策略**：Test-First  
> 理由：純函式的輸入輸出明確。

**優先級**：P0  
**相關功能**：Story 7  
**依賴關係**：無
