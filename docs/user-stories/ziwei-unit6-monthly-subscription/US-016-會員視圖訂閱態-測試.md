# US-016：會員視圖訂閱態 測試

**作為** 開發者  
**我想要** 先有會失敗的 `resolveMembershipView` 訂閱分支測試  
**以便** 訂閱有效時畫面旗標正確

**輸入格式**：
- `lib/membership/view.ts`、`view.test.ts`
- spec §2 Story 7 的 view 規則

**輸出格式**：
- 擴充 `lib/membership/view.test.ts`

**驗收條件**：
- [x] 聚焦測試因功能尚未實作而預期紅燈（失敗原因是功能缺失，不是語法或 import 錯誤）
- [x] `hasActiveSubscription && isOwnReport` 時：`unlockMode="subscription"`、`advancedLocked=false`、`showCta=false`、`showUnlockWithPoint=false`、`pointsInsufficient=false`、`showPointsPackCta=true`
- [x] 有 `subscriptionActiveUntil` 時，`ctaLabel` 為「訂閱有效至 YYYY/MM/DD」（以 Asia/Taipei 格式化）；沒有時為「訂閱有效中」
- [x] 判斷順序為 lifetime → points → subscription；lifetime 或 points 同時成立時不走 subscription
- [x] 訂閱失效時回到單元 5 的鎖定分支（S7-6：點數 ≥ 1 時顯示「用點解鎖」）；此條屬回歸，可能已是綠的

#### 驗收說明

**整體結論**：PREPARED：預期紅燈測試已建立

> `npx vitest run lib/membership/view.test.ts`：20 tests，3 failed／17 passed。待 US-017 轉綠。

- 路徑：`lib/membership/view.test.ts` 新增 `describe("subscription state (unit 6 US-016)")`，共 7 支
- 紅燈原因（3 支，都是功能缺失）：
  - 訂閱有效時 `unlockMode` 仍為 `none`
  - `ctaLabel` 仍是「永久解鎖完整報告」，不是「訂閱有效至 2026/10/18」（UTC 16:30 換算成台北時間是隔天）或「訂閱有效中」
- 已經是綠的回歸斷言：lifetime 優先、points 優先、非自己的報告不解鎖、訂閱失效後回到用點解鎖（S7-6）
- 不是語法、import 或型別錯誤（vitest 不做型別檢查；新欄位要到 US-017 才會加進型別）

**測試策略**：Test-First（測試準備）  
> 理由：純函式，輸入輸出明確。

**優先級**：P0  
**相關功能**：Story 7  
**依賴關係**：無
