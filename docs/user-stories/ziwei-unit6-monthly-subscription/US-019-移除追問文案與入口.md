# US-019：移除追問文案與入口

**作為** 會員／學員  
**我想要** 畫面上完全沒有追問輸入框、按鈕或「可追問」文案  
**以便** 不會誤以為月費包含聊天功能

**輸入格式**：
- `components/report/CommercialSecondaryZone.tsx`、`ReportCard.tsx`、`lib/commercial/preview.ts`、`lib/membership/view.ts`、`lib/constants.ts`

**輸出格式**：
- 上述檔案與受影響的測試

**驗收條件**：
- [x] S9-1：任何狀態下，DOM 都沒有 `slot-followup`、「送出追問」，也沒有 `slot-subscribe` 的「即將開放」按鈕
- [x] S9-2：`rg -n "追問" app components lib --glob '!*.test.*'` 沒有結果，或只剩「不做追問」的註解
- [x] 刪除 `FOLLOWUP_*`、`PREVIEW_MONTHLY_REMAINING`、`SUBSCRIBE_LABEL`／`SUBSCRIBE_HINT`；`MODE_SUBSCRIBE_LINE` 改為「訂閱：有效期間內可看進階報告」；`MODE_UNLOCK_LINE` 去掉「不附贈追問」
- [x] 移除 `followupLocked`／`followupCaption`／`subscribeLabel` 欄位，`typecheck` 通過

#### 驗收說明

**整體結論**：PASS ✅

> 追問 UI、欄位與常數已全部移除，`CommercialSecondaryZone` 只保留三種收費模式的說明。全套 vitest 419 passed／1 skipped；lint、typecheck 都通過。

---

**AC-1：S9-1 任何狀態下 DOM 都沒有追問或訂閱預告**

狀態：✅ 通過

- `components/report/CommercialSecondaryZone.tsx` 已改寫，拿掉 input、送出按鈕、「了解訂閱權益」按鈕，也移除 state
- `ReportCard.test.tsx` 新增 `it.each(A～D)`，斷言以下元素都不存在：`slot-followup`、`slot-subscribe`、「送出追問」、「了解訂閱權益」、「追問」與「即將開放」字樣

---

**AC-2：S9-2 `rg "追問"` 無結果**

狀態：✅ 通過

- `rg -n "追問" app components lib --glob '!*.test.*'` 的 exit code 為 1，代表沒有命中

---

**AC-3：刪除常數並改寫文案**

狀態：✅ 通過

- `lib/constants.ts` 刪除：
  - `FOLLOWUP_PLACEHOLDER`、`FOLLOWUP_HINT`、`FOLLOWUP_API_UNIMPLEMENTED`
  - `PREVIEW_MONTHLY_REMAINING`、`PREVIEW_NO_DEDUCT`、`SUBSCRIBE_ACTIVE_PREVIEW`
  - `SUBSCRIBE_LABEL`、`SUBSCRIBE_HINT`、`UPCOMING_UNLOCK_NOTE`
  - slot `followup`、`subscribe`
- 文案改寫：`MODE_SUBSCRIBE_LINE` 改為「訂閱：有效期間內可看進階報告」；`MODE_UNLOCK_LINE` 去掉「不附贈追問」

---

**AC-4：移除 `followupLocked`／`followupCaption`／`subscribeLabel` 欄位**

狀態：✅ 通過

- `lib/commercial/preview.ts`、`lib/membership/view.ts`、`components/report/ReportCard.tsx` 都已移除這些欄位
- 預覽態 C／D 原本只差在追問文案，現在與 B 相同（`preview.test.ts` 已斷言）
- `tsc --noEmit` 通過

---

**說明**：預覽 C 送出追問、預覽 D 訂閱預覽文案、鎖定狀態點追問出現「即將開放」，這 3 支舊測試驗證的是被刪除的功能，已一併移除。

**測試策略**：Test-After  
> 理由：屬文案與元件刪除，用 DOM 斷言加 grep 驗收即可。

**優先級**：P0  
**相關功能**：Story 9  
**依賴關係**：US-018
