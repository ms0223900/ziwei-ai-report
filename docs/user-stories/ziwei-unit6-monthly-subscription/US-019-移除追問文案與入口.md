# US-019：移除追問文案與入口

**作為** 會員／學員  
**我想要** 畫面上完全沒有追問輸入框、按鈕或「可追問」文案  
**以便** 不會誤以為月費包含聊天功能

**輸入格式**：
- `components/report/CommercialSecondaryZone.tsx`、`ReportCard.tsx`、`lib/commercial/preview.ts`、`lib/membership/view.ts`、`lib/constants.ts`

**輸出格式**：
- 上述檔案與受影響的測試

**驗收條件**：
- [ ] S9-1：任何狀態下，DOM 都沒有 `slot-followup`、「送出追問」，也沒有 `slot-subscribe` 的「即將開放」按鈕
- [ ] S9-2：`rg -n "追問" app components lib --glob '!*.test.*'` 沒有結果，或只剩「不做追問」的註解
- [ ] 刪除 `FOLLOWUP_*`、`PREVIEW_MONTHLY_REMAINING`、`SUBSCRIBE_LABEL`／`SUBSCRIBE_HINT`；`MODE_SUBSCRIBE_LINE` 改為「訂閱：有效期間內可看進階報告」；`MODE_UNLOCK_LINE` 去掉「不附贈追問」
- [ ] 移除 `followupLocked`／`followupCaption`／`subscribeLabel` 欄位，`typecheck` 通過

**測試策略**：Test-After  
> 理由：屬文案與元件刪除，用 DOM 斷言加 grep 驗收即可。

**優先級**：P0  
**相關功能**：Story 9  
**依賴關係**：US-018
