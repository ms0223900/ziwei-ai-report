# US-023：交棒與文件更新

**作為** 單元 7／8 實作者  
**我想要** 一份可讀欄位清單，並把專案文件的範圍更新為已含訂閱  
**以便** 後續單元不必回頭猜資料結構

**輸入格式**：
- spec §2 Story 12；US-022 的 howto

**輸出格式**：
- `howto-monthly-subscription.md` 交棒段落
- `docs/spec.md`、`docs/architecture.md`

**驗收條件**：
- [ ] S12-1：列出單元 7 只讀欄位（`plan_id`、`status`、起訖、最近事件的 `event_type`／`idempotency_key`／`processed_at`）與單元 8 可重用的素材
- [ ] `docs/spec.md` 的 Won't Have 與「不建 subscriptions」兩段加註「單元 6 起覆寫」
- [ ] `docs/architecture.md` 補上兩張新表與 period-webhook 路由
- [ ] 不改 `AGENTS.md`（見 issues 檔問題 5）

**測試策略**：Test-After  
> 理由：文件交付，不需要自動化測試。

**優先級**：P2  
**相關功能**：Story 12  
**依賴關係**：US-019、US-022
