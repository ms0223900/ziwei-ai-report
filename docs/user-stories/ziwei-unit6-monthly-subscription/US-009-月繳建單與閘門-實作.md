# US-009：月繳建單與閘門 實作

**作為** 已登入會員  
**我想要** 只送方案 ID 就能建月繳單，重複建單會被擋下  
**以便** 不會同時有兩份綠界定期定額合約

**輸入格式**：
- US-008 的紅燈測試

**輸出格式**：
- `app/api/payments/checkout/route.ts`
- `lib/errors.ts`／`lib/constants.ts`：新錯誤碼與繁中訊息

**驗收條件**：
- [x] US-008 測試轉綠
- [x] 閘門用 `eq` 取出訂閱與 pending 月繳單，在 JS 以 `Date` 比較時間
- [x] 缺 `PeriodReturnURL` 時，在寫入 `orders` **之前**就回傳
- [x] 閘門查詢出錯時採 fail-closed，回可讀的繁中錯誤，不建單
- [x] 定期定額欄位要在計算 `CheckMacValue` **之前**加入
- [x] 建單時**不**插入 `subscriptions`
- [x] 終身與點數包的既有測試全綠

#### 驗收說明

**整體結論**：PASS ✅

> checkout 支援月繳：先檢查 `PeriodReturnURL`，再過兩道閘門，最後在計算 CheckMacValue 之前加入定期定額欄位。全套 vitest 369 passed／1 skipped，lint、typecheck 都通過。

---

**AC-1：US-008 測試轉綠**

狀態：✅ 通過

- `npx vitest run app/api/payments/checkout/route.test.ts`：21 passed（原本 6 failed）

---

**AC-2：閘門以 `eq` 取列，在 JS 用 `Date` 比較**

狀態：✅ 通過

- `app/api/payments/checkout/route.ts` 的 `monthlyCheckoutBlocked()`：
  - 以 `eq(user_id)` 取訂閱，比較 `current_period_end >= now`
  - 以三個 `eq`（user、plan、pending）列出訂單，判斷是否在 5 分鐘內（`PENDING_MONTHLY_WINDOW_MS`）

---

**AC-3：缺 `PeriodReturnURL` 時在寫 `orders` 之前回傳**

狀態：✅ 通過

- `isMonthly && !env.periodReturnUrl` 時直接回 `paymentUnavailableError()`；S1-5 測試同時斷言月繳沒有寫入訂單，點數包仍回 200

---

**AC-4：閘門查詢出錯時 fail-closed**

狀態：✅ 通過

- 任一查詢回 error 就視為擋下，回 409 `SUBSCRIPTION_IN_PROGRESS`（「已有有效訂閱或訂單處理中，請稍後再試。」），不建單
- **說明**：錯誤分支沒有獨立測試（fake 無法注入查詢錯誤），靠程式閱讀確認

---

**AC-5：定期定額欄位在 CheckMacValue 之前加入**

狀態：✅ 通過

- `"period" in plan` 時才寫入 `PeriodAmount`／`PeriodType`／`Frequency`／`ExecTimes`／`PeriodReturnURL`，位置在 `computeCheckMacValue()` 之前；S1-1 用 `verifyCheckMacValue` 驗過

---

**AC-6：建單時不插入 `subscriptions`**

狀態：✅ 通過

- route 只 insert `orders`，沒有寫入 `subscriptions`

---

**AC-7：終身與點數包既有測試全綠**

狀態：✅ 通過

- 原本 11 支 checkout 測試全數通過；新增的閘門只在 `isMonthly` 時執行

**測試策略**：Test-First  
> 理由：對 US-008 的紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 1／2  
**依賴關係**：US-008
