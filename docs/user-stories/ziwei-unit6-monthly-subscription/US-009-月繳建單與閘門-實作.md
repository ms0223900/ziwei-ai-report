# US-009：月繳建單與閘門 實作

**作為** 已登入會員  
**我想要** 只送方案 ID 就能建月繳單，重複建單會被擋下  
**以便** 不會同時有兩份綠界定期定額合約

**輸入格式**：
- US-008 的紅燈測試

**輸出格式**：
- `app/api/payments/checkout/route.ts`
- `lib/errors.ts`／`lib/constants.ts`：新錯誤碼與繁中訊息

**驗收條件**：
- [ ] US-008 測試轉綠
- [ ] 閘門用 `eq` 取出訂閱與 pending 月繳單，在 JS 以 `Date` 比較時間
- [ ] 缺 `PeriodReturnURL` 時，在寫入 `orders` **之前**就回傳
- [ ] 閘門查詢出錯時採 fail-closed，回可讀的繁中錯誤，不建單
- [ ] 定期定額欄位要在計算 `CheckMacValue` **之前**加入
- [ ] 建單時**不**插入 `subscriptions`
- [ ] 終身與點數包的既有測試全綠

**測試策略**：Test-First  
> 理由：對 US-008 的紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 1／2  
**依賴關係**：US-008
