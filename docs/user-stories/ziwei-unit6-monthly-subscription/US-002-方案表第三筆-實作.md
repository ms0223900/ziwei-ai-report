# US-002：方案表第三筆 實作

**作為** 系統  
**我想要** 伺服器端方案表有第三筆月繳方案，env 可組出 PeriodReturnURL  
**以便** 建單只信任後端常數

**輸入格式**：
- US-001 的紅燈測試
- `plans.ts` 是 `server-only`，client 不可 import

**輸出格式**：
- `lib/payments/plans.ts`：`CheckoutPlan` union 擴充為三型
- `lib/payments/checkout-env.ts`：新增 `periodReturnUrl`
- `.env.example` 新增 `ECPAY_PERIOD_RETURN_URL=`，並加入 `lib/security/secrets-not-leaked.test.ts` 的 `ECPAY_EXAMPLE_KEYS`

**驗收條件**：
- [x] US-001 測試轉綠
- [x] `readEcpayCheckoutEnv()` 缺 `periodReturnUrl` 時**不**回 null，避免終身與點數包建單失效
- [x] `npm run typecheck` 通過

#### 驗收說明

**整體結論**：PASS ✅

> 方案表新增 `subscribe_report_monthly`（19／TWD／紫微斗數月繳訂閱／M·1·12）；`readEcpayCheckoutEnv()` 新增 `periodReturnUrl`。全套 vitest：331 passed／1 skipped；lint、typecheck 乾淨。

---

**AC-1：US-001 測試轉綠**

狀態：✅ 通過

- `npx vitest run lib/payments/plans.test.ts lib/payments/checkout-env.test.ts`：7 passed（原本 4 failed）
- `lib/payments/plans.ts` 的 `resolveCheckoutPlan()` 認得 `SUBSCRIBE_REPORT_MONTHLY_PLAN_ID`，並回傳 `period`

---

**AC-2：`readEcpayCheckoutEnv()` 缺 `periodReturnUrl` 時不回 null**

狀態：✅ 通過

- `lib/payments/checkout-env.ts` 的 null 判斷條件仍只看 `merchantId`、`returnUrl`、`clientBackUrl`；`periodReturnUrl` 缺值時為空字串
- `checkout-env.test.ts` 的「keeps existing plans usable…」案例驗證此行為

---

**AC-3：`npm run typecheck` 通過**

狀態：✅ 通過

- `tsc --noEmit` 無錯誤；`CheckoutPlan` union 第三型不影響 `app/api/payments/checkout/route.ts` 既有的欄位存取
- 另外 `.env.example` 已新增 `ECPAY_PERIOD_RETURN_URL=`，並加入 `secrets-not-leaked.test.ts` 的 `ECPAY_EXAMPLE_KEYS`（對應輸出格式）

**測試策略**：Test-First  
> 理由：對 US-001 的紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 1  
**依賴關係**：US-001
