# US-017：會員視圖訂閱態 實作

**作為** 系統  
**我想要** 讓會員視圖支援「訂閱有效」這個第四態  
**以便** 訂閱會員不會看到解鎖或買點的主 CTA

**輸入格式**：
- US-016 的紅燈測試

**輸出格式**：
- `lib/membership/view.ts`、`lib/constants.ts`（新文案）

**驗收條件**：
- [x] US-016 測試轉綠
- [x] `ResolveMembershipViewInput` 新增 `hasActiveSubscription?` 與 `subscriptionActiveUntil?`
- [x] 既有的 view 測試全綠

#### 驗收說明

**整體結論**：PASS ✅

> `resolveMembershipView()` 新增 subscription 分支。全套 vitest 412 passed／1 skipped，lint、typecheck 都通過。

---

**AC-1：US-016 測試轉綠**

狀態：✅ 通過

- `npx vitest run lib/membership/view.test.ts`：20 passed（原本 3 failed）
- `lib/membership/view.ts`：subscription 分支排在 lifetime、points 之後，條件是 `isOwnReport && hasActiveSubscription`。`ctaLabel` 由 `subscriptionCtaLabel()` 以 `Intl.DateTimeFormat`（`timeZone: "Asia/Taipei"`）格式化成 `YYYY/MM/DD`；沒有日期時顯示「訂閱有效中」

---

**AC-2：Input 新增 `hasActiveSubscription?`、`subscriptionActiveUntil?`**

狀態：✅ 通過

- `ResolveMembershipViewInput` 已加入這兩個欄位；`MembershipUnlockMode` 新增 `"subscription"`；文案常數 `MEMBERSHIP_CTA_SUBSCRIPTION_*` 放在 `lib/constants.ts`

---

**AC-3：既有 view 測試全綠**

狀態：✅ 通過

- 既有 13 支 view 測試與全套測試都通過；沒傳新欄位時，行為與單元 5 相同

**測試策略**：Test-First  
> 理由：對 US-016 的紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 7  
**依賴關係**：US-016
