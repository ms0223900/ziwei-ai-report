# US-008：月繳建單與閘門 測試

**作為** 開發者  
**我想要** 先有會失敗的月繳建單與閘門測試  
**以便** 定期定額參數與防重複建單規則被鎖住

**輸入格式**：
- `app/api/payments/checkout/route.ts`、`route.test.ts`（已有 `setPaymentEnv`）
- spec §2 Story 1／2；AC S1-1～S2-4

**輸出格式**：
- 擴充 `app/api/payments/checkout/route.test.ts`

**驗收條件**：
- [x] 聚焦測試因功能尚未實作而預期紅燈（失敗原因是功能缺失，不是語法或 import 錯誤）
- [x] S1-1：`fields` 含 `TotalAmount=19`、`PeriodAmount=19`、`PeriodType=M`、`Frequency=1`、`ExecTimes=12`、`PeriodReturnURL`、`ChoosePayment=Credit`，且 `CheckMacValue` 可驗；`orders` 新增一筆 pending、金額 19
- [x] S1-2：前端夾帶的金額或週期被忽略；S1-3：未登入回 401，`orders` 沒有新增；S1-4：已永久解鎖仍回 200
- [x] S1-5：保留 `ECPAY_RETURN_URL`／`ECPAY_CLIENT_BACK_URL`、清掉 `APP_BASE_URL` 與 `ECPAY_PERIOD_RETURN_URL` 時，月繳回 `PAYMENT_UNAVAILABLE` 且 `orders` 沒有新增；點數包仍回 200
- [x] S2-1：期末未過（`active` 或 `past_due`）時回 409 並帶繁中訊息，`orders` 沒有新增
- [x] S2-2：5 分鐘內有 pending 月繳單時回 409；用假時鐘推進超過 5 分鐘後回 200；5 分鐘內只有 pending **點數包**單時不擋月繳
- [x] S2-3：已取消或已到期時回 200；S2-4：未知 plan 回 400 `UNSUPPORTED_PLAN`
- [x] 備註：S1-4／S2-4 等可能在 US-002 完成後就已經是綠的，只有新行為的斷言需要先紅

#### 驗收說明

**整體結論**：PREPARED：預期紅燈測試已建立

> `npx vitest run app/api/payments/checkout/route.test.ts`：21 tests，6 failed／15 passed。待 US-009 轉綠。

- 路徑：`app/api/payments/checkout/route.test.ts` 新增 `describe("… subscribe_report_monthly")`，共 10 個 it／13 支測試，用 `vi.useFakeTimers({ toFake: ['Date'] })` 控制時間
- `setPaymentEnv()` 預設新增 `ECPAY_PERIOD_RETURN_URL`，並清空 `APP_BASE_URL`；`PAYMENT_ENV_KEYS` 同步納入，每支測試結束後還原
- 紅燈原因（6 支），都是功能缺失：
  - S1-1、S1-2：`fields` 沒有定期定額參數
  - S1-5：缺 `PeriodReturnURL` 時仍回 200
  - S2-1：active 或 past_due 且期末未過時仍回 200
  - S2-2：5 分鐘內有 pending 月繳單時仍回 200
- 已經是綠的回歸斷言：S1-3（401）、S1-4（永久解鎖可訂閱）、S2-3（取消或到期後可再建單）、pending 點數包不擋月繳、S2-4（400）
- 不是語法、import 或環境錯誤

**測試策略**：Test-First（測試準備）  
> 理由：API 的輸入輸出與閘門條件都很明確。

**優先級**：P0  
**相關功能**：Story 1／2  
**依賴關係**：US-002、US-005
