# US-001：方案表第三筆 測試

**作為** 開發者  
**我想要** 先有會失敗的月繳方案與 PeriodReturnURL 設定測試  
**以便** 金額、週期、品名只由後端決定

**輸入格式**：
- `lib/payments/plans.ts`：目前只有終身／點數包
- `lib/payments/checkout-env.ts`：沒有 `periodReturnUrl`
- 定稿：`subscribe_report_monthly`、19／TWD／「紫微斗數月繳訂閱」、`M`／`1`／`12`

**輸出格式**：
- 擴充 `lib/payments/plans.test.ts`
- 新增或擴充 checkout-env 測試

**驗收條件**：
- [ ] 聚焦測試因功能尚未實作而預期紅燈（失敗原因是功能缺失，不是語法／import 錯誤）
- [ ] 斷言 `resolveCheckoutPlan("subscribe_report_monthly")` 回 19／TWD／品名與 `period: {periodType:"M", frequency:1, execTimes:12}`
- [ ] 斷言終身 99、點數包 49 維持不變；未知 plan 回 `null`
- [ ] 斷言 `ECPAY_PERIOD_RETURN_URL` 優先，否則 `APP_BASE_URL + /api/payments/ecpay/period-webhook`；兩者皆無時 `periodReturnUrl` 為空，且不影響既有欄位

**測試策略**：Test-First  
> 理由：資料表與 env 組合規則明確，可直接寫成 input/output 斷言。

**優先級**：P0  
**相關功能**：Story 1；spec §2 Story 1  
**依賴關係**：無
