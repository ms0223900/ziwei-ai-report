# US-008：共用常數與錯誤碼

**作為** 系統  
**我想要** disclaimer、locked_fields、錯誤碼、鎖定說明與高風險五句同一來源  
**以便** API 與畫面不各寫一套

**輸入格式**：
- disclaimer：`本結果僅供娛樂與自我反思，不作為醫療、法律、財務、投資或重大人生決策依據。`
- LockCaption：`解鎖進階命書後，即啟七日行事方針與吉凶路徑析理`
- `locked_fields`：`["action_plan","path_compare","rationale"]`
- error_code：`VALIDATION_ERROR`／`SCHEMA_INVALID`／`GENERATION_FAILED`／`PERSIST_FAILED`／`HIGH_RISK` 與 AI spec 對應繁中 message（422／502／503 短句不變）
- 高風險正文（與 spec／`.pen` Note 同句）：
  - `health`：這題涉及健康與醫療判斷，我不能用命盤作答。請尋求合格醫療專業人員協助。
  - `legal`：這題涉及法律諮詢，我不能用運勢作答。請尋求合格律師或法律援助。
  - `financial_risk`：這題涉及財務與投資決策，我不能用命盤或運勢給判斷。請尋求合格的金融／專業意見。
  - `pregnancy`：這題涉及孕產與身體安全，我不能用命盤作答。請尋求合格醫療專業人員協助。
  - `self_harm`：若你正處於危險中，請立即尋求現場專業協助。命盤解讀不能處理這類情況。

**輸出格式**：
- `lib/constants.ts`
- `lib/errors.ts`（可對應 HTTP status 的錯誤型別）

**驗收條件**：
- [x] disclaimer 與 spec／`.pen` Disclaimer 同句（含「投資」）
- [x] LockCaption 與 spec／`.pen` 同句（舊 brief「解鎖進階報告後可見 7 天…」不得再出現）
- [x] 五類高風險正文與 spec 固定句一致，可供 US-007／US-021 引用
- [x] `locked_fields` 固定為進階三欄
- [x] 422／502／503 預設 message 與 spec 表一致
- [x] 本任務不實作 Route Handler

#### 驗收說明

**整體結論**：PASS ✅

> 文案與 error_code 集中在 `lib/constants.ts`／`lib/errors.ts`；`scanHighRisk` 改讀同一份高風險句。

---

**AC-1：disclaimer 含「投資」且與 spec 同句**

狀態：✅ 通過

- `lib/constants.ts` 的 `DISCLAIMER` 與 spec／`.pen` 同句

---

**AC-2：LockCaption 對齊 `.pen`，無舊 brief 句**

狀態：✅ 通過

- `LOCK_CAPTION` 為「解鎖進階命書後，即啟七日行事方針與吉凶路徑析理」
- `lib/` 無「解鎖進階報告後可見」

---

**AC-3：五類高風險正文供 US-007／US-021 引用**

狀態：✅ 通過

- `HIGH_RISK_MESSAGES` 與 spec Story 4 同句
- `lib/policy/high-risk.ts` 的 `scanHighRisk()` 改引用此表

---

**AC-4：`locked_fields` 固定進階三欄**

狀態：✅ 通過

- `LOCKED_FIELDS` = `action_plan`／`path_compare`／`rationale`

---

**AC-5：422／502／503 預設 message 與 spec 一致**

狀態：✅ 通過

- `lib/errors.ts` 的 `schemaInvalidError`／`generationFailedError`／`persistFailedError` 對應 422／502／503 與 spec 短句

---

**AC-6：不實作 Route Handler**

狀態：✅ 通過

- 未新增 `app/api/**`

**測試策略**：Test-After
> 理由：常數對齊，無狀態轉換；後續 API／UI 測試會引用這些值。

**優先級**：P0  
**相關功能**：Story 3／4／5  
**依賴關係**：無
