# US-014：遮罩組裝測試

**作為** 開發者  
**我想要** 先有會失敗的遮罩測試  
**以便** 對外永遠看不到進階欄

**輸入格式**：
- 函式契約（尚未實作）：`lib/masking/buildReportResponse.ts`
- 輸入：已通過驗證的完整／分層物件 + meta（`status`、`generation_status`）
- 禁止出現於 body：`advanced_json`、`rationale`、`path_compare`、`action_plan`

**輸出格式**：
- `lib/masking/buildReportResponse.test.ts`
- 對齊 AI spec 成功 Response 形狀（`rpt_demo_001` 示範）

**驗收條件**：
- [x] 聚焦測試因功能尚未實作而預期紅燈
- [x] 斷言 200 body 含 basic 淺層 + `disclaimer` + `locked_fields` + `status=basic` + `generation_status=success`
- [x] 斷言 body **不含** 進階三欄與 `advanced_json`
- [x] `tier` 對外為 `basic`

#### 驗收說明

**整體結論**：PREPARED：預期紅燈測試已建立

> `npx vitest run lib/masking/buildReportResponse.test.ts` 因 `Cannot find module './buildReportResponse'` 失敗（功能尚未實作）。待 US-015 轉綠。

---

**AC-1：因功能尚未實作而預期紅燈**

狀態：✅ 通過

- 失敗訊息：`Cannot find module './buildReportResponse' imported from /workspace/lib/masking/buildReportResponse.test.ts`

---

**AC-2：200 body 含 basic 淺層與 persist meta**

狀態：✅ 通過

- `lib/masking/buildReportResponse.test.ts` 對齊 AI spec HTTP 200（`rpt_demo_001`）
- 斷言 `disclaimer`、`locked_fields`、`status=basic`、`generation_status=success`

---

**AC-3：body 不含進階三欄與 advanced_json**

狀態：✅ 通過

- 斷言不具 `rationale`／`path_compare`／`action_plan`／`advanced_json`
- 傳入 complete 物件與 `advanced_json` 仍不得外洩

---

**AC-4：tier 對外為 basic**

狀態：✅ 通過

- 輸入 `tier: "advanced"` 時仍斷言 `body.tier === "basic"`

**測試策略**：Test-First（測試準備）
> 理由：遮罩是明確的物件轉換。

**優先級**：P0  
**相關功能**：Story 2b  
**依賴關係**：US-010
