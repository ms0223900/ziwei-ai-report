# US-014：遮罩組裝測試

**作為** 開發者  
**我想要** 先有會失敗的遮罩測試  
**以便** 對外永遠看不到進階欄

**輸入格式**：
- 函式契約（尚未實作）：`lib/masking/buildReportResponse.ts`
- 輸入：已通過驗證的完整／分層物件 + meta（`status`、`generation_status`）
- 禁止出現於 body：`advanced_json`、`rationale`、`path_compare`、`action_plan`

**輸出格式**：
- `lib/masking/buildReportResponse.test.ts`
- 對齊 AI spec 成功 Response 形狀（`rpt_demo_001` 示範）

**驗收條件**：
- [ ] 聚焦測試因功能尚未實作而預期紅燈
- [ ] 斷言 200 body 含 basic 淺層 + `disclaimer` + `locked_fields` + `status=basic` + `generation_status=success`
- [ ] 斷言 body **不含** 進階三欄與 `advanced_json`
- [ ] `tier` 對外為 `basic`

**測試策略**：Test-First（測試準備）
> 理由：遮罩是明確的物件轉換。

**優先級**：P0  
**相關功能**：Story 2b  
**依賴關係**：US-010
