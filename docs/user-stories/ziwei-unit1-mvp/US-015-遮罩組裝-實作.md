# US-015：遮罩組裝實作

**作為** 系統  
**我想要** 唯一對外組裝只回未付款 basic  
**以便** 日後解鎖不必重跑模型也能藏進階

**輸入格式**：
- 通過 ajv 的分層／完整 JSON + 寫入後 meta
- Mock `report_id` 可維持 `rpt_demo_001`（與 DB uuid 分開）

**輸出格式**：
- `lib/masking/buildReportResponse.ts`
- 回傳物件對齊 spec 成功 Response

**驗收條件**：
- [x] US-014 測試轉綠
- [x] 此函式是唯一組 HTTP 200 報告 body 的地方（程式註解標明）
- [x] 不呼叫 LLM
- [x] 不把完整 schema 套用在輸出 body 上當驗收

#### 驗收說明

**整體結論**：PASS ✅

> allowlist 組出 spec HTTP 200；`advanced_json` 與進階三欄不進 body。Route 尚未接上（US-018）。

---

**AC-1：US-014 測試轉綠**

狀態：✅ 通過

- `npx vitest run lib/masking/buildReportResponse.test.ts` 3 passed
- 全庫 `npx vitest run` 40 passed；`npm run typecheck`／`npm run lint` 通過

---

**AC-2：唯一組 HTTP 200 報告 body**

狀態：✅ 通過

- `lib/masking/buildReportResponse.ts` 檔頭註明唯一組裝處
- repo 內無其他 `buildReportResponse`／HTTP 200 報告組裝

---

**AC-3：不呼叫 LLM**

狀態：✅ 通過

- 模組無 `fetch`／OpenRouter；只讀 report 欄位與常數

---

**AC-4：不以 complete schema 驗輸出**

狀態：✅ 通過

- 實作未 import `validateComplete`／ajv；輸出以 allowlist 組成，不以 complete 當驗收

**測試策略**：Test-First
> 理由：對 US-014 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 2b  
**依賴關係**：US-014
