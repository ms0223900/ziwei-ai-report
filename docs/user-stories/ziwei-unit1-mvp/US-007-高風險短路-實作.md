# US-007：高風險短路實作

**作為** 訪客  
**我想要** 輸入觸及高風險時只看到固定安全回覆  
**以便** 系統不編命盤、不呼叫 LLM、不寫成功報告

**輸入格式**：
- 請求內字串（至少 `nickname`、`focus`）
- 可維護關鍵字表（投資、自殺、懷孕、律師、病症等明確詞；不掃單字「金」）

**輸出格式**：
- `lib/policy/high-risk.ts`：未命中回 `null`；命中回 `{ category, message }`
- 不在此任務呼叫 OpenRouter 或 insert

**驗收條件**：
- [x] US-006 測試轉綠
- [x] 命中不產生命盤結論字串
- [x] 五類 message 與 spec 固定句一致
- [x] 匯出的函式可供 Route Handler 在生成前呼叫

#### 驗收說明

**整體結論**：PASS ✅

> `scanHighRisk` 在生成前可呼叫；命中只回 category／固定句，不編命盤。

---

**AC-1：US-006 測試轉綠**

狀態：✅ 通過

- `npx vitest run lib/policy/high-risk.test.ts` 9 passed
- `lib/policy/high-risk.ts` 的 `scanHighRisk()` 未命中回 `null`

---

**AC-2：命中不產生命盤結論字串**

狀態：✅ 通過

- 回傳只有 `category` 與 `message`；無 overall／盤面欄位

---

**AC-3：五類 message 與 spec 固定句一致**

狀態：✅ 通過

- health／legal／financial_risk／pregnancy／self_harm 文案與 AI spec Story 4 同句

---

**AC-4：可供 Route Handler 在生成前呼叫**

狀態：✅ 通過

- 匯出純函式 `scanHighRisk`，無 OpenRouter、無 insert

**測試策略**：Test-First
> 理由：對 US-006 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 4  
**依賴關係**：US-006
