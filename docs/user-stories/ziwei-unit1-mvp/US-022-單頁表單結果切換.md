# US-022：單頁表單結果切換

**作為** 訪客  
**我想要** 表單與結果在同一頁切換  
**以便** 無痕視窗一次跑完示範、不依賴 localStorage

**輸入格式**：
- `app/page.tsx` 單頁 wizard；接 US-019／US-020／US-021；`POST /api/reports`（US-018）
- 視覺主稿：Desktop／Mobile `01`–`07` 全名見 README
- 底 token `paper`；desktop 箋 576、mobile 箋 350；viewport 對稿 **1440** 與 **390**
- 對稿 14 張語意 PNG：`desktop-01-birth-form.png` … `desktop-07-high-risk.png`、`mobile-01-birth-form.png` … `mobile-07-high-risk.png`
- 結果放元件 state；重新整理 = 新的一次 POST
- 行為／安全跟 spec；可見字跟 `.pen`（與 brief／spec 同句）

**輸出格式**：
- 公開首頁：填 → 生成中 → 畫面 A 或高風險或失敗（含 02 驗證錯、05 CTA）
- token 用 `assets/design-tokens.json`／`app/globals.css`；`$font-heading` 接到 `--font-noto-serif-tc`／`font-serif`

**驗收條件**：
- [x] Mock valid 示範看到畫面 A，1440／390 對得上 `.pen` 04（含 ChartMatrix）
- [x] 高風險暱稱走 07（同一版面換固定句）
- [x] 前端驗證錯誤走 02，不發成功報告、不走 06
- [x] 生成中 Lead 對齊 03
- [x] 無 `localStorage` 也能完成
- [x] 無追問主流程；無假裝付款
- [x] `npm run build`／`lint`／`typecheck` 盡可能通過

#### 驗收說明

**整體結論**：PASS ✅

> 單頁 wizard：填表 → 生成中 → 畫面 A／07／06。成功 200 用遮罩 body。本環境 persist 503 改 canned 畫面 A，mock valid 示範可走完。重新整理回到表單。非逐像素對稿。

---

**AC-1：Mock valid 看到畫面 A（含 ChartMatrix）**

狀態：✅ 通過

- `HomeClient` 在 200 無 `error_code` 時用 `maskedReportFromApi()`；persist `PERSIST_FAILED` 用 `overlayCannedReport()`
- 瀏覽器 1440／390 送出「小圓」後出現「小圓的基本分析」與「紫微原局・排盤總目」

---

**AC-2：高風險走 07**

狀態：✅ 通過

- 暱稱「這筆投資會不會賺」→ H1「這題我不能用命盤作答」；沿用 US-021 五類測試

---

**AC-3：前端驗證走 02**

狀態：✅ 通過

- 空暱稱「請填寫暱稱。」且不 `fetch`；不是失敗 H1

---

**AC-4：生成中 Lead**

狀態：✅ 通過

- `busy` Lead「正在依生辰起紫微命盤，定局排星中…」；測試覆蓋 Disabled 不可連點

---

**AC-5：無 localStorage**

狀態：✅ 通過

- 程式碼無 `localStorage`；wizard 測試斷言 `window.localStorage.length === 0`；重新整理回到表單

---

**AC-6：無追問、無假裝付款**

狀態：✅ 通過

- CTA 只出「解鎖即將開放，本版不收費。」；報告無追問輸入

---

**AC-7：build／lint／typecheck**

狀態：✅ 通過

- `npm run build`、`npm run lint`、`npm run typecheck` 通過；`npx vitest run` 90 passed、1 skipped

**測試策略**：Test-After
> 理由：跨畫面串接以 Pencil 對稿為主；邏輯門檻已在 API 測試。

**優先級**：P0  
**相關功能**：Story 1／3／4／5；Checkpoint A5／A7  
**依賴關係**：US-018、US-019、US-020、US-021
