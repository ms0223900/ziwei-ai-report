# US-003：會員環境變數與機密守門

**作為** 開發者  
**我想要** env 留位與機密守門涵蓋 grant secret  
**以便** 前端 bundle 不會出現 service role 或 grant secret

**輸入格式**：
- 變數：`MEMBERSHIP_GRANT_SECRET`、`MEMBERSHIP_GRANT_ENABLED`（server-only）
- 既有：`NEXT_PUBLIC_SUPABASE_ANON_KEY` 本單開始給 client 用
- `lib/security/secrets-not-leaked.test.ts` 的 `SECRET_ENV_KEYS`

**輸出格式**：
- `.env.example` 新增上述 server 變數（無真實值）
- 機密守門測試列入 `MEMBERSHIP_GRANT_SECRET`
- 禁止 `NEXT_PUBLIC_MEMBERSHIP_GRANT_SECRET`

**驗收條件**：
- [x] `.env.example` 有 `MEMBERSHIP_GRANT_SECRET`、`MEMBERSHIP_GRANT_ENABLED`
- [x] 守門測試把 `MEMBERSHIP_GRANT_SECRET` 當禁止出現在 client
- [x] 無任何 `NEXT_PUBLIC_*` 帶 grant secret 或 service role

#### 驗收說明

**整體結論**：PASS ✅

> `npx vitest run lib/security/secrets-not-leaked.test.ts` 通過。本 Phase 只留位，尚未建 grant route。

---

**AC-1：.env.example 留位**

狀態：✅ 通過

- `.env.example` 新增空的 `MEMBERSHIP_GRANT_SECRET` 與 `MEMBERSHIP_GRANT_ENABLED=0`

**AC-2：守門名單含 grant secret**

狀態：✅ 通過

- `SECRET_ENV_KEYS` 列入 `MEMBERSHIP_GRANT_SECRET`；bundle 掃描也禁止該字串

**AC-3：無 NEXT_PUBLIC grant／service role**

狀態：✅ 通過

- 斷言無 `NEXT_PUBLIC_MEMBERSHIP_GRANT_SECRET`；source regex 含 `MEMBERSHIP`

**測試策略**：Test-After
> 理由：改 env 範例與既有守門名單，補跑現有測試即可。

**優先級**：P0  
**相關功能**：Story 2b／7  
**依賴關係**：無
