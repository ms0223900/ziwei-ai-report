# US-016：會員三態視圖 實作

**作為** 系統  
**我想要** 用純函式算出訪客／未開通／已開通視圖  
**以便** UI 不把 `COMMERCIAL_PREVIEW` 當成已開通

**輸入格式**：
- US-015 紅燈測試
- 不讀 `advanced_json` 以外的預覽假文來充 unlocked
- 不另造 `slot-upgrade`；未開通用 `slot-unlock-cta`

**輸出格式**：
- 純函式檔（建議 `lib/membership/view.ts`）
- 不 fetch、不寫 DB

**驗收條件**：
- [x] US-015 測試轉綠
- [x] `unlocked` 覆蓋預覽鎖定與 `EXAMPLE_BLOCKS`
- [x] 函式不讀 storage、不碰 Route

#### 驗收說明

**整體結論**：PASS ✅

> `npx vitest run lib/membership/view.test.ts` → 5 passed。純函式，不 fetch、不讀 storage。

---

**AC-1：US-015 轉綠**

狀態：✅ 通過

- `lib/membership/view.ts` 的 `resolveMembershipView()` 依 session／`access_status` 回傳標題、CTA、advanced

**AC-2：unlocked 覆蓋預覽**

狀態：✅ 通過

- `previewEnabled`＋`previewState=B` 仍用傳入真文；JSON 不含「預覽用範例」

**AC-3：不讀 storage、不碰 Route**

狀態：✅ 通過

- 檔案無 `localStorage`／`fetch`／`cookies`；只算旗標

**測試策略**：Test-First
> 理由：對 US-015 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 6；spec 第 7 節問題 5  
**依賴關係**：US-015
