# US-016：會員三態視圖 實作

**作為** 系統  
**我想要** 用純函式算出訪客／未開通／已開通視圖  
**以便** UI 不把 `COMMERCIAL_PREVIEW` 當成已開通

**輸入格式**：
- US-015 紅燈測試
- 不讀 `advanced_json` 以外的預覽假文來充 unlocked
- 不另造 `slot-upgrade`；未開通用 `slot-unlock-cta`

**輸出格式**：
- 純函式檔（建議 `lib/membership/view.ts`）
- 不 fetch、不寫 DB

**驗收條件**：
- [ ] US-015 測試轉綠
- [ ] `unlocked` 覆蓋預覽鎖定與 `EXAMPLE_BLOCKS`
- [ ] 函式不讀 storage、不碰 Route

**測試策略**：Test-First
> 理由：對 US-015 紅燈實作至綠。

**優先級**：P0  
**相關功能**：Story 6；spec 第 7 節問題 5  
**依賴關係**：US-015
