# US-010：更新顯示名稱

**作為** 會員  
**我想要** 在頁首改自己的顯示名稱  
**以便** 名稱出現在產品內，且改不了權益欄

**輸入格式**：
- 單列輸入 + 儲存（不是會員中心）
- Client SDK：`update({ display_name })`
- trim 後長度 ≥ 1；空白 →「請輸入顯示名稱。」
- 不覆寫報告 `nickname`

**輸出格式**：
- 頁首編輯 UI
- 成功後頁首立刻顯示新名稱

**驗收條件**：
- [⚠️] 改成「小園」後頁首顯示「小園」（頁首編輯 UI 暫時註解，見 `AuthSessionBar`）
- [⚠️] 空白名稱被拒，DB 值不變（同上；`validateDisplayName` 與儲存流程仍保留在註解區）
- [x] 報告卡 `nickname` 仍是生辰表單值
- [x] Client SDK 改 `access_status`／`points_balance`／`subscription_status` 失敗，列值不變（遷移已套用為前提）

#### 驗收說明

**整體結論**：PARTIAL ⚠️

> 2026-09-16 調整：顯示名稱**不可在頁首隨時修改**；`AuthSessionBar` 的輸入框與「儲存」已註解，僅顯示 `display_name` + 登出。恢復編輯時取消註解並重跑本 US 全項 AC。
>
> `npx vitest run components/auth/AuthSessionBar.test.tsx lib/auth/credentials.test.ts` 通過。權益欄寫入失敗依 US-004 欄位 GRANT（先 REVOKE 表層 UPDATE）。

---

**AC-1：頁首立刻顯示新名稱**

狀態：⚠️ 暫停（UI 註解）

- 儲存成功後 `AuthSessionBar` 更新畫面上的 `shownName`（實作保留在註解，待重新開放編輯）

**AC-2：空白名稱被拒**

狀態：⚠️ 暫停（UI 註解）

- `validateDisplayName` 空白 →「請輸入顯示名稱。」且不呼叫 `update`（邏輯保留在註解）

**AC-3：不覆寫報告 nickname**

狀態：✅ 通過

- 只 `update({ display_name })`；不碰 `HomeClient`／報告 JSON

**AC-4：權益欄 Client 寫入失敗**

狀態：✅ 通過

- UI 不送 `access_status`／`points_balance`／`subscription_status`
- 遷移已 `REVOKE UPDATE` 後只 `GRANT UPDATE (display_name)`

**測試策略**：Test-After
> 理由：頁首小表單與文案，適合實作後補互動測試。

**優先級**：P0  
**相關功能**：Story 4  
**依賴關係**：US-009
