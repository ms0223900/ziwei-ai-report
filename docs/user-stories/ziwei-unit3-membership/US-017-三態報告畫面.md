# US-017：三態報告畫面

**作為** 求測者  
**我想要** 在同一份報告功能看到對應身分的內容與下一步  
**以便** 能分辨免費、未開通與已開通，且已開通也不附贈追問

**輸入格式**：
- 主驗收路徑：**先處於該身分，再生成**（不要用訪客離頁登入後的舊 `persist_id`）
- 訪客：基本摘要＋鎖定槽＋即將開放
- 已登入 `locked`：鎖定；點 `slot-unlock-cta` 顯示「開通由講師受控流程處理，本版不收費」；不改 DB
- 已登入 `unlocked`：POST 後用 `persist_id` GET，三槽填真文（含 7 天），不是「預覽用範例」
- 登出：丟掉 GET payload，三槽回佔位，頁首回 `slot-auth-entry`
- 追問框始終不可送出（本版）

**輸出格式**：
- 改 `HomeClient`／`ReportCard`／`AdvancedLockedPanel` 接 US-016 旗標與 GET
- 真文渲染 `path_compare` 物件與 `action_plan` 陣列（不要壓成單段預覽假文）

**驗收條件**：
- [x] 訪客報告頁無進階真文，CTA 仍不收費
- [x] 未開通 GET 失敗時畫面不露出三欄；點 CTA 不改 `access_status`
- [x] 已開通標題為「小圓的進階報告」（示範資料），可見 7 天計畫真文
- [x] 已開通＋預覽 B 仍是真文，不能用預覽假文驗收「已開通」
- [x] 登出後進階真文從畫面消失
- [x] 已開通追問仍鎖定；`points_balance` 不因開通變成正數

#### 驗收說明

**整體結論**：PASS ✅

> `npx vitest run components/home/HomeClient.test.tsx components/report/ReportCard.test.tsx` 通過。本包未對遠端 Auth＋GET 做 live 開通驗收。

---

**AC-1：訪客無真文、CTA 不收費**

狀態：✅ 通過

- 預設 `HomeClient` 走訪客 membership；CTA 仍「解鎖完整報告／即將開放」

**AC-2：未開通不露三欄、CTA 不改權益**

狀態：✅ 通過

- locked 不打 GET；點「升級／開通」只顯示講師受控提示，`fetch` 仍 1 次

**AC-3：已開通標題與 7 天真文**

狀態：✅ 通過

- POST 後 GET `persist_id`；標題「小圓的進階報告」，`action_plan` 含「第 1 天」

**AC-4：預覽 B 不能當已開通**

狀態：✅ 通過

- unlocked membership 覆蓋 preview B，畫面無「預覽用範例」

**AC-5：登出清真文**

狀態：✅ 通過

- `initialHasSession` 變 false 時 `advanced` 派生為 null，標題回基本分析

**AC-6：追問仍鎖定、不贈點**

狀態：✅ 通過

- unlocked 時 `followupLocked` 仍 true；本包不寫 `points_balance`

**測試策略**：Test-After
> 理由：報告箋接線與登出清欄屬 UI 整合，適合實作後補畫面測試。

**優先級**：P0  
**相關功能**：Story 1／6／7；spec 第 7 節問題 3／5  
**依賴關係**：US-009、US-014、US-016
