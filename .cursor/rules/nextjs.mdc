---
description: PROTOTYPE Next.js—inline fetch OK in early delivery; server-only secrets; webpack scripts.
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# Next.js（PROTOTYPE）

## 目標

- **快**：先讓頁面與 API 可驗收；可暫時在 route 或單一檔內寫 fetch／parse。
- **build/dev 一律 `--webpack`**（與藍本一致）。

## 不可妥協

- **`NEXT_PUBLIC_*`** 不得放 service role、OpenRouter key、ECPay HashKey/HashIV。
- 需 token 的邏輯只在 **server**（Route Handler / Server Action）。
- Next 16：`await cookies()`；本版不讀 session。

## App Router

- 需要 `useState`、瀏覽器 API → `'use client'`。
- Route Handler：**盡量**回一致 JSON／HTTP status；來不及時也需 **不洩 secret**。
- 對外組裝只走遮罩層；**永不**把 `advanced_json` 回給前端。
- Tailwind：**可**將樣式寫在同一元件檔；另拆 CSS module 非必須。

## Do / Don't

| Do | Don't |
| --- | --- |
| 機密與 mutations 留 server | client 直接呼叫 OpenRouter |
| API 失敗有繁中提示 | 白屏／uncaught |
| 重複邏輯第三次再抽 | 一開始就過度分層 |

## 參照

- [`docs/spec.md`](docs/spec.md)、[`AGENTS.md`](AGENTS.md)
